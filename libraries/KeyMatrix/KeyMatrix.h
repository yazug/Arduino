/* *  KeyMatrix.h *   * *  Created by â∫âí ê^àÍ on 11/03/18. *  Copyright 2011 __MyCompanyName__. All rights reserved. * */#include <WProgram.h>#ifndef KeyMatrix_H#define KeyMatrix_Hstruct KeyMatrix {  boolean buttonDown;  const char * row_pins, * col_pins;  byte row_size, column_size;  const char * keytops;    KeyMatrix(const char rows[], const char cols[], const char keys[]);    char getKey(boolean hold = false);  };#endif